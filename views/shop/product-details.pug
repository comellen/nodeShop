extends ../includes/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/product.css")

block content
    main.centered
        h1 #{product.title}
        hr
        div
            img(src="/assets/mysterybox.png", alt=product.title + " image").product-item
            //- img(src="#{product.imgUrl}" alt="#{product.title}")
        h2 $#{product.price}
        p #{product.description}
        include ../includes/add-to-cart.pug